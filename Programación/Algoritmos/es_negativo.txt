Pedir número y llamarlo a
Si no es número:
    Volver a pedir el número, explicando que el valor introducido no es un número
LLamarlo a
Si a>0:
    Decir que es positivo(Fin)
Si a=0:
    Decir que es cero(Fin)
Decir que es negativo(Fin)
